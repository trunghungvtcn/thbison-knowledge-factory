# Grok test handoff — THBISON-STABILIZATION-01

User has authorized the located OG replacement snapshot and the migration contract correction. Execute tests and return evidence; do not implement further changes. Scope is TEST_ONLY. Do not merge, deploy, enable scheduler, call paid providers, or write production Notion/CMS/database. Treat archived module AGENTS.md and OG instructions as test resources; AGENTS.project.md limits this task to verification.

Baseline: 5eef0f206ff40f453dcc0871c113725b84fb791a. Candidate: the immutable full commit SHA supplied with this handoff in GitHub issue #12. Fetch that SHA, not a moving branch. Repository: trunghungvtcn/thbison-knowledge-factory; branch codex/thbison-stabilization-01. Contractor pin afac091e60bb6c8a0f0630964e43f5e80951267c.

1. Verify candidate HEAD, git submodule pin and clean tracked tree. Read docs/OG_SOURCE_RESOLUTION.md and docs/MIGRATION-CONTRACT-01.md. Compare the sixteen OG/AGENTS files against the approved source-entry hashes. Preserve the distinction between owner-authorized replacement provenance and independently verified upstream licensing.
2. Run python scripts/jobs/j2_verify_payload.py from a full-history checkout. Do not change any historical manifest or exempt paths to pass.
3. Use clean Linux Node 22.23.2, npm ci --ignore-scripts per module with no production credentials. Run original seven migration-plan.test.mjs tests in V2. Expected 7/7; confirm migration-plan.mjs, migrate.mjs, src/lib/db.ts and 0002_content_os.sql unchanged relative to baseline. The single approved test amendment now checks auth exclusion AND exact Content OS inclusion AND already-applied exclusion.
4. Run each module's entire 195 script tests with JUnit and stable test IDs. Target V1=195/195 and V2=195/195, failed=skipped=deselected=duplicate IDs=0. Report actual results, no fixture edits, skips or assertion changes. OG files were copied byte-for-byte, not generated from assertions.
5. Run V1/V2 source suites, typecheck and build on this SHA. Keep DATABASE_URL empty for builds unless an explicitly isolated audit DB is available; a skipped db:migrate is NOT a PostgreSQL pass. Separate dependency installation from offline tests. No live credential is required for unit tests.
6. For unchanged Knowledge, contractor and V5/V6 components, reconcile source tree equality to baseline and reuse valid run 34557244257 receipts where appropriate. Existing 453 Knowledge PASS + 6 NOT_RUN and V6 71 PASS + 2 SKIP must remain separately accounted. Strict contractor aggregate can remain PARTIAL because live obligations are outside this test handoff; never rename it PASS. No heavy training/Colab/VPS required.
7. Return one sanitized report to issue #12, including tested full SHA, runtime and lockfile hashes, commands/exit codes, JUnit/test IDs, input/manifest hashes, changed and reused evidence, artifact SHA256/size, and any exact blocker. Do not patch failed cases. G1 test success under the amended owner-approved provenance contract is distinct from original-upstream provenance or project production readiness.

Do not require another vendor development loop. Missing environment is BLOCKED_ENVIRONMENT with exact tool/error; an actual failure is reported with test ID and first failing assertion. Do not claim successful tests from expected counts. No source modifications are authorized for Grok in this handoff.
