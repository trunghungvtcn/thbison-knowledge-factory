# Unpacked guidance packs

These directories are the unzipped contents of the three GitHub Release assets.
Binary `*.zip` files are gitignored (`*.zip` in repo `.gitignore`).

Download the ZIPs from the GitHub Release (not from git) and verify:

    sha256sum -c SHA256SUMS

This is a GitHub-required prompt kit. It is not offline-complete.
