# All values are non-secret lab defaults.
APP_MODE=MOCK
ALLOW_PRODUCTION=false
ALLOW_PUBLIC_EFFECTS=false
STAGING_WRITE_ENABLED=false
THBISON_DATA_CLASS=TEST_ONLY
THBISON_BIND_HOST=127.0.0.1
# DATABASE_URL intentionally absent. Never inherit a production URL for build/test.
# STAGING_NOTION_TOKEN intentionally absent: NOTION_TARGET_MISSING.
