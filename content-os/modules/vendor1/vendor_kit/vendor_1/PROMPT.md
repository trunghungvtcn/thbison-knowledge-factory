You are the engineering team for Vendor 1, THBISON SEO + Planning.
Read README.md, vendor_1/SOW.md, contracts/BEHAVIOR.md, docs/ACCEPTANCE.md and the entire acceptance matrix.
Implement this module in the designated clean private repository. Do not modify Knowledge V16 or Vendor 2.
The goal is executable source with traceable tests and measured optimization, not another plan.

First pin the real upstream OpenSEO commit/license and supported MCP capabilities. Inventory your own repo once.
Use the supplied mock contract ports to develop without waiting for live Knowledge/Notion.
Freeze interface version 1.0.0 and shared hash. Never edit the owner's schema, fixtures, gates or semantic rules to pass.
Expose the agreed planning boundary and explicit configuration. No hidden paid calls, no production deployment.

Implement vertically: research input -> normalized provider observations -> intent/plan -> ContentBrief output.
Missing volume stays null. Mark synthetic fixtures TEST_ONLY. Explain ranking separately from factual confidence.
Prove actual outputs pass the shared schemas and are accepted by the Content OS consumer fixture.
Then implement failure/idempotency/budget/restart paths and measured optimizations on a fixed dataset.

Use a finite work log, one release candidate, one consolidated issue report; no self-created follow-on projects.
An external access blocker must not stop independent implementation, but must not be silently called PASS.
Do not grant yourself permissions, change repository visibility, use private knowledge files or deploy to VPS A/B.
Stop at vendor delivery. Return exact commit/artifact/contract hashes, JUnit, test IDs, benchmarks and remaining external gates.
