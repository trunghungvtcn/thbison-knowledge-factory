# THBISON Vendor 3 Runtime / Orchestrator (remediation)

Offline-verified mock runtime. Contract 1.0.0 unmodified. acceptance_claimed=false.

    PYTHONPATH=. python -m pytest tests
    python tools/staging_preflight.py

See FIX_RESPONSE.md, STATE_PROJECTION.md, RUNBOOK.md.
